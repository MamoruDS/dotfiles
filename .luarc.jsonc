{
    "$schema": "https://raw.githubusercontent.com/sumneko/vscode-lua/master/setting/schema.json",
    "runtime.version": "LuaJIT",
    "workspace": {
        "library": [
            // nvim --headless -c 'lua print(vim.inspect(vim.api.nvim_get_runtime_file("lua", true)))' -c 'qa!'
        ]
    }
}